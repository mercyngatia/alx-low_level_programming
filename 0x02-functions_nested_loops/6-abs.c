#include "main.h"
/**
 * _abs - return abs value of integer
 * @n: the integer to print
 * Return: absolute value
 */
int _abs(int)
{

if (n < 0)
{
return (n * (-1));
}

else if (n == 0)
{
return (0);
}

else
{
return (n);
}

}

