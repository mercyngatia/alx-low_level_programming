#include "main.h"
/**
 * _islower - checks lowercase letter
 * @c: character to check the case
 * REturn: 0 or 1
 */
int _islower(int c)
{

if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}

}
