#include <stdio.h>
/**
 * main - Main entry
 * DEscription: Printsall single digit numbers of base 10
 * start from 0, followed by a new line.
 * Return: 0
 */
int main(void)

{
int single_digit:
for (single_digit = 0; single-digit < 10; single_digit++)
{
printf("%single_digit", single_digit);
}

print("\n");

return (0);

}
