#include <stdio.h>
/**
 * Main - main entry
 * Desc: Get random number and check its last digit, compare with 5
 * Return: 0
 */
int main(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}

putchar('\n');

return(0);
}
