#include "main.h"
/**
 * puts2 - prints every character of a string
 * @str: pointer to an int that will be changed
 * Return: void - correct
 */

void puts(char *str)
{
int x;

for (x = 0; str[a] != '\0'; a++)
{
if (a % 2 == 0)
_putchar(str[a]);
}

_putchar('\n');
}
